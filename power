#include <iostream>
#include<math.h>

using namespace std;

int main()
{
    int x,y;
    cout<<"Enter any number = ";
    cin>>x;
    cout<<"Enter any number = ";
    cin>>y;
    int power=pow(x,y);
    cout<<"Power of x is = "<<power;
    return 0;
}
