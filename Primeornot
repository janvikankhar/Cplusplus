#include <iostream>

using namespace std;

int main()
{
    int n,i;
    cout<<"Enter any number = ";
    cin>>n;
    for(i=2;i<n;i++){
      if(n%2==0){
        cout<<"Is not prime";
        break;
      }
}
    if(i==n)
       cout<<"Is Prime";
    return 0;
}
