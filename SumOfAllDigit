#include<iostream>
using namespace std;

int main()
{
    long int a,num,sum=0;
    cout<<"Enter any integer :: ";
    cin>>num;

    while(num>0)
    {
        a=num%10;
        num=num/10;
        sum=sum+a;
    }
    
    cout<<"Sum of Digits of a Number = "<<sum;

    return 0;
}
